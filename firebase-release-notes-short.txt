v0.2.77 — error-reduction punch list

Big internal hardening drop. Nothing user-facing should feel different; you should be able to ride exactly as before.

Under the hood:
- Unified API client (apiFetch + mobileApiFetch). All 65 endpoint calls now share one timeout / retry / typed-error path.
- Android native fatal tagging — crashes that fire before the JS bundle finishes loading now carry the right "preview" tag so we can triage them.
- ConnectivityMonitor + offline sync touched indirectly via the new error envelope; behaviour preserved.

User-visible changes (small):
- Privacy & analytics consent SPLIT. Crash reports default ON (legitimate-interest, to help us fix bugs); product analytics defaults OFF, opt-in. If you already made a choice, it's preserved.
- Privacy policy page updated to match.

Please confirm:
- Cold-launch reaches route-planning within ~3s
- Plan + start + end a ride works unchanged
- Profile → Privacy & analytics: both toggles visible, crash on by default, analytics off by default (fresh install only; existing testers keep their saved values)
- Community feed, leaderboard, trophy case all load normally (these endpoints were touched by the API client migration)
- Hazard reporting + voting still feels normal

If anything crashes or feels off, screenshot + tell me which screen you were on.

Feedback to the early-access invitation email.
