# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
# Metaspace bumped 512m -> 1024m on 2026-05-01: KSP processing of the
# expo-updates / expo-manifests dependency tree blew through 512m and the
# daemon entered a fatal OutOfMemoryError loop mid-build. Heap also raised
# 2g -> 4g to give Kotlin / R8 / dexing more room.
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# --- R8: code shrinking / obfuscation / optimisation -------------------
# Play Console rated bundle 133 (v0.2.130) "App optimisation: Low":
# obfuscation 2%, shrinking and optimisation unreported, 48.5 MB
# uncompressed DEX. Cause: these two properties had never been set
# anywhere, so `minifyEnabled` in app/build.gradle resolved to its own
# `false` default and R8 has never run on a release build in this
# project's history.
#
# Applies to the `release` build type only, across all three flavors.
# Debug is untouched. Keep rules live in app/proguard-rules.pro, which is
# gitignored by the blanket `android` rule and force-added, and which MUST
# stay in the sync list in scripts/build-preview.sh -- otherwise the
# production build at C:\dpb runs R8 with no project rules at all.
android.enableMinifyInReleaseBuilds=true
android.enableShrinkResourcesInReleaseBuilds=true

# R8 full mode is AGP 8's default and is deliberately DISABLED for the
# first minified release. Full mode adds assumptions (non-null returns for
# un-kept classes, aggressive generic-signature stripping) that are the
# most common source of release-only crashes in a React Native app. Ship
# one proven build, then try deleting this line and re-verify on device.
android.enableR8.fullMode=false

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
# Disabled for local dev builds — bridgeless mode can't load Metro bundle on Windows.
# Preview/production builds via EAS or build-preview.sh use new arch via app.config.ts.
newArchEnabled=false

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Use this property to enable edge-to-edge display support.
# This allows your app to draw behind system bars for an immersive UI.
# Note: Only works with ReactActivity and should not be used with custom Activity.
edgeToEdgeEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Network inspector — kept off by default to ensure release builds never ship
# with the inspector port surface exposed. Developers who want the inspector
# can override locally with `-PEX_DEV_CLIENT_NETWORK_INSPECTOR=true` on the
# gradle command line, or set it in `~/.gradle/gradle.properties`. Audit
# script `scripts/audit-release-artifacts.sh` enforces this on release builds.
EX_DEV_CLIENT_NETWORK_INSPECTOR=false

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false

expo.sqlite.enableFTS=true
expo.sqlite.useSQLCipher=false