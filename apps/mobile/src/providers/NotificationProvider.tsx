/**
 * Headless provider that manages push notification lifecycle.
 * Currently disabled — requires native rebuild with expo-notifications.
 * Re-enable when EAS project ID is configured and native modules are available.
 */
export const NotificationProvider = () => null;
