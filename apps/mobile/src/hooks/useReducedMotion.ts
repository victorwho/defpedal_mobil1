/**
 * useReducedMotion — convenience re-export
 *
 * The canonical implementation lives in the design-system hooks directory.
 * This re-export allows non-design-system code (screens, lib utilities)
 * to import the hook without reaching into the design-system internals.
 */
export { useReducedMotion } from '../design-system/hooks/useReducedMotion';
